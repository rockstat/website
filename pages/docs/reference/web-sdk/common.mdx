import asset from 'next/asset'
import { withDoc } from 'app/lib'
import { Alert, Tip, Info } from 'app/components'

export const meta = {
  title: '',
}

# На самом деле это шаблон!

Да, так и есть. Когда нужно создать страницу, сначала копируются этот шаблон на ее место, а уже потом заполняется информация.
А может и не заполниться...

<Alert>Страница не страница, вся информация на ней ложь!</Alert>

#### Про что не забыть:

- [ ] HTTP(без s) по дефолту не поддерживается
- [ ] Разные транспорты
- [ ] Websocket. С websdk и без
- [ ] ...


export default withDoc({...meta})(({children}) => <>{children}</>)
