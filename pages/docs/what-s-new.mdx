import asset from 'next/asset'
import { withDoc } from 'app/lib'
import { Tip } from 'app/components'

export const meta = {
  title: '',
}

# What's new!

### (coming)

- **docs**
  - в **Архитектура / Жизненный цикл и адресация** появилось описание принципов формирования ключей событий
- **infrastructue**
  - Обновился список параметов **Web-SDK** на странице [Getting Started](/docs/intro/getting-started)
  - Новая локация пользовательских образов контейнеров (my_images в thaia).
  Нужно перенести самостоятельно, при помощи последовательности действий:
  `NL=/srv/platform/images/user && shopt -s dotglob && mkdir -p $NL && mv /srv/platform/data/images/* $NL`.
  При ошибке `mv: cannot stat '/srv/platform/data/images/*': No such file or directory`,
  ничего не делайте, это нормально, значит переносить нечего.

  - Anaconda container пребилд, включающий набор дополнитеольных библиотек
  - В Theia пользователь получил фиксированные идентификаторы.`UID=765 GID=765`,
    в дальнешем к этим идентификаторам будут приведены все кастомные конфигурационные файлы и кастомные образы,
    что позволит управлять конфигурацией системы из theia.
  - httpdebug service. Управлять где ему запустить можно переменной `httpdebug_bind` вида `80:8080` или `addr:80:8080`.
  Для остановки указать `-e http_debug_bind=absent`
- **bootstrap**
  - Переназначать используемый образ можно через `images_extra`.
  - Переназначать переменные окружения для старта новых контейнеров
    (которые запускаются при установке) можно через `containers_env_extra`



### 14 sep 2018

- **infr** В графане изначально добавлен ClickHouse datasource, а также уже встроен демо-дашбород.
- **dash** Реструктуризация кода Dashboars для дальнейшей разработки интерфейсов управления контейнерами
- **band** Новый API band framework: `@dome` больше нет, теперь функции помечаются так:
    - `@expose()` - делает метод доступным другим сервисам, но не регистрируется в Front
    - `@expose.handler()` - регистрация обработчиком в Front
    - `@expose.enricher(keys=['in.gen.track'], props=dict(ip='td.ip'))` - регистрация обогатителя в Front - пример обогатителя из сервиса mmgeo
    - `@export.listener()` - регистрация слушателя в Front
    - `@worker()` - регистрация воркером сервиса
    - `@cleaner()` - регистрация чистильщиком севиса, будет запущет при остановке.
- Ebaloger: новый сервис в составе. Отбирает у докера логи контейнеров, принимает другую часть через сокет, стримит все это добро на вронтенд в app.
- **dash** Конечно же фронтовое приложение получение и отображения логов, заточено под большое кол-во данных
- **infr** На 90% завершен переход на предварительно собранные образы сервисов. Собираются в открытом виде в travis-ci
- **bootstrap** Новый способ получения SSL. Раработана новая роль для ансибл, она использует альтенативный клиент acme.sh, и умеет получать
  wildcard сертификаты (*.вашдомен), причем делать это не вмешиваясь в работу сервера, используя DNS-challenge.

### 06 sep 2018

- Band / Новый синтакс logger. Никуда не деаться, режим совместимости не работает корректно `logger.info('extract result %s', out)` должго превратиться в `logger.info('extract result', out=out)`
- Много что переведено на пердварительную сборку образов

### 27 aug 2018

- **band** Вместо переменной `CH_DSN`, указывающей на ClickHouse Proxy теперь есть `CHS_DSN`, указывающая на ClickHouse Server и `CHP_DSN` ClickHouse Proxy
- **band** Родительскиф образ для сервисов на Band Framework теперь `rockstat/band-base-py` (вместо `rst/band-base-py`)
- **band** Изменился базоывый образ у сервисов на питоне. Вмето `rst/band-base-py` будеи использоваться `madiedinro/band-base.py`
- **band** Появился промежуточный образ графаны, при помощи которого можно вносить изменения в ее сборку


### 25 aug 2018s

- **bootstrap** main version switched to master.

### 15 aug 2018

- **bootstrap** clickhouse column `channel` changed from enum to string. Only `events`, `webhooks`, `activity` tables updated automatically, others need to be updated. Look at `clickhouse_migrations/002-...`


**... a lot of issues**

export default withDoc({...meta})(({children}) => <>{children}</>)
