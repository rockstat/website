{
  "bodyContent": "# Микросервисная архитектура с Band\n\n\n\n\n## Протокол взаимодействия\n\n### JSON-RPC2 over Redis PubSub\n\nСтруктура сообщения\n\nОбрабатывается библиотеками, в функции передается только результат\n\n\n### Ответы к Frontier service\n\n```typescript\n\ninterface DispatchResult {\n  location?: string;\n  error?: string;\n  errorCode?: number;\n  [k: string]: any;\n}\n\n```\n\nОбработка результата:\n\n1. Если имеется error, будет возвращена ошибка с http кодом из `errorCode` или подставлен кол по умолочанию `500`\n1. Если имеется `location` будет совершен редирект в эту локацию. Всегда используется `302` код\n1. Во всех остальных случаях ответ будет возвращен полностью",
  "bodyHtml": "<h1>Микросервисная архитектура с Band</h1>\n<h2>Протокол взаимодействия</h2>\n<h3>JSON-RPC2 over Redis PubSub</h3>\n<p>Структура сообщения</p>\n<p>Обрабатывается библиотеками, в функции передается только результат</p>\n<h3>Ответы к Frontier service</h3>\n<pre><code class=\"hljs\"><span class=\"hljs-keyword\">interface</span> DispatchResult {\n  location?: <span class=\"hljs-built_in\">string</span>;\n  error?: <span class=\"hljs-built_in\">string</span>;\n  errorCode?: <span class=\"hljs-built_in\">number</span>;\n  [k: <span class=\"hljs-built_in\">string</span>]: <span class=\"hljs-built_in\">any</span>;\n}</code></pre><p>Обработка результата:</p>\n<ol>\n<li>Если имеется error, будет возвращена ошибка с http кодом из <code>errorCode</code> или подставлен кол по умолочанию <code>500</code></li>\n<li>Если имеется <code>location</code> будет совершен редирект в эту локацию. Всегда используется <code>302</code> код</li>\n<li>Во всех остальных случаях ответ будет возвращен полностью</li>\n</ol>\n",
  "preview": "Микросервисная архитектура с Band\n\n Протокол взаимодействия\n\nJSON-RPC2 over Redis PubSub\n\nСтруктура",
  "title": "Микросервисная архитектура с Band",
  "dir": "content/docs/micro",
  "base": "band-overview.json",
  "ext": ".json",
  "sourceBase": "band-overview.md",
  "sourceExt": ".md"
}